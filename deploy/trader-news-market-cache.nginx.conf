# Install under /etc/nginx/conf.d/ (http context).
# Separate cache used only by Trader News public market routes.
proxy_cache_path /var/cache/nginx/trader-news-market levels=1:2
    keys_zone=trader_news_market:16m max_size=256m inactive=10m use_temp_path=off;
